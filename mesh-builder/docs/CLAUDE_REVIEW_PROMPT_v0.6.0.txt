You are Claude acting as the independent adversarial code, geometry and release reviewer for Project SkyForge Mesh Builder Sidecar v0.6.0 CR-0011 remediation.

One outer ZIP is supplied. Its outer SHA-256 is supplied separately in Marty's message. Compute it before extracting and stop with an MBS-GATE document if it differs.

Inside, verify:

- reviewed base candidate `BASE_REVIEWED/SkyForge_Mesh_Builder_Sidecar_v0.6.0_CR0011.zip` SHA-256:
  c6dd2d463e5f5ab39573877a89aa1325d46e7daec7b416afbe896f5eba1ffe61
- remediation candidate `CANDIDATE/SkyForge_Mesh_Builder_Sidecar_v0.6.0.zip` SHA-256 must match the value asserted by the outer `CLAUDE_REVIEW_BINDING_v0.6.0_CR0011_REMEDIATION.json`, its adjacent sidecar, and `SHA256SUMS.txt`.
- Compute that candidate hash independently before reading any narrated closure claim.

MBS-CR-0011 is included. Continue review numbering as MBS-CR-0012 and continue new findings after MBS-95. Do not accept narrated closure claims without executing or independently measuring them.

Allowed scope remains deterministic v0.6.0 geometry and verification only. Provider adapters, Meshy, Hunyuan, multiview, thrusters, fallback geometry, animation, destruction and gameplay integration remain prohibited. Cost governance, budgets, spend ledger, cache, Keychain, prompt governance, authority identity, coordinate contract and Blender/export gates must not be weakened.

Required verification:

1. Confirm the source diff against the exact CR-0011-reviewed candidate. The expected diff is 4 added, 0 removed, 12 changed and 175 byte-identical files.
2. Verify all nine pinned dependencies exactly.
3. Run `ruff check .` with Ruff 0.15.22.
4. Run the complete pytest suite. The expected collection is 99 tests; require 99 passed, 0 skipped, 0 failed and 0 errors.
5. Independently reproduce the three candidate fixture GLBs and all geometry metrics.
6. Re-measure lower-field independence. Acceptance requires relative residual >= 0.10. Narrated values are 0.230694, 0.234321 and 0.247570.
7. Confirm the lower field uses a distinct exponent plus lower-only keel, belly and chine terms, and that image-centre `fliplr` mirroring is absent.
8. Confirm the field fixture's 16 sub-threshold holes / 38 pixels are measured and filled, its reloaded GLB has Euler 2 and genus 0, and a larger intentional hole is retained and fails `genusMax`.
9. Prove the negative controls fail when source/reload audits disagree and when reloaded-GLB component count is changed.
10. Verify topology metrics are measured from the independently reloaded GLB in target coordinates.
11. Verify opaque-mask albedo influence is reported as `foreground_mask_derivation_only` and alpha-mask influence as `none`.
12. Verify adaptive export-grid selection keeps a wide planform below 180,000 triangles and records the chosen grid.
13. Review the recalibrated gates and state whether the documented margins are defensible.
14. Verify two independently built source ZIPs are byte-identical, closing MBS-94.
15. Prove geometry tests make no paid OpenAI transport calls.
16. Run archive hygiene and transactional sealer tests.
17. Run the live Blender neutral/bank/silhouette/export preflight if Blender is available. The supplied `Run v0.6.0 Live Blender Preflight.command` and `scripts/run_live_blender_preflight.py` must be reviewed for safety and exact-candidate binding.

Explicitly disposition MBS-85 through MBS-95. Pay particular attention to:

- MBS-85: Ruff must actually be green.
- MBS-86: determine whether the new lower basis is genuinely independent rather than another restatement.
- MBS-87: confirm no threshold-speckle tunnels survive.
- MBS-88: mutation/negative-control coverage must now fail the regressions.
- MBS-89: reporting must be path-dependent and truthful.
- MBS-90: adaptive grid must not weaken identity or topology.
- MBS-91: thresholds must be calibrated rather than merely tightened.
- MBS-92: all adjacent topology metrics must have one independent source.
- MBS-93: forced image-centre mirroring must be absent.
- MBS-94: seals must be byte-reproducible.
- MBS-95: live Blender evidence remains mandatory before Marty performs subjective user testing.

If Blender is unavailable but every source, Ruff, pytest and geometry check is green, state that the code review is accepted subject only to the automated live Blender preflight. Do not ask Marty to perform subjective testing. Identify exactly which files the automated preflight must return for your final one-minute clearance.

Deliver one checksum-bound MBS-CR-0012 review and state clearly whether user testing is cleared, conditionally cleared after automated Blender preflight, or withheld.
