# v0.7.1 exact-candidate sealing notice

This tree is not user-testing clearance. The v0.7.1 hotfix candidate must be sealed together with the
separately packaged Sprite Foundry Import Probe v0.1.1 and the exact reviewed v0.7.0 candidate as its
implementation baseline.

Use `Seal v0.7.1 for Claude.command` only after both source bindings are current. The command creates
separate clean producer and probe environments, runs full Ruff and zero-skip tests, captures primary
release-reproducibility evidence, proves MBS-155 rejection before provider execution, runs the
mandatory three-fixture live Blender preflight, executes the public zero-credit Meshy diagnostic,
and assembles one checksum-bound package only when every gate is green.

Paid provider work remains prohibited. MBS-136 and MBS-150 through MBS-154 remain open.
