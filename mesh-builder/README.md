# SkyForge Mesh Builder Sidecar v0.7.1 — authority-suitability hotfix candidate

v0.7.1 is a focused fail-closed hotfix over the exact Claude-reviewed v0.7.0 source. It preserves the
provider-neutral local path, frozen VMP v1 producer, independent Import Probe boundary, geometry,
Blender, Keychain, OpenAI cost-governance, and no-paid-provider posture.

## v0.7.1 changes

- Rejects perspective, diagonal, rotated, fragmented, clipped, or materially asymmetric manual
  authority uploads before job allocation, provider execution, Blender launch, or API spend.
- Requires explicit manual-authority certification and records a machine-readable suitability report
  in every accepted job.
- Re-verifies complete governed beauty-to-authority lineage, including profile, asset ID, filenames,
  image records, and SHA-256 bytes.
- Carries the exact MBS-155 user-reported three-quarter beauty as a rejecting regression fixture.
- Documents the registered interceptor profile and `0.952496` forward Blender IoU baseline (MBS-144).
- Emits primary double-build release-reproducibility evidence for producer and Import Probe source
  and release archives (MBS-145).
- Expands byte-identity governance evidence from six to twelve protected modules (MBS-146).
- Hardens Import Probe archive path-collision diagnostics (MBS-147).
- Ships the effective SF-AM-0001 governing document, not only its amendment wrapper (MBS-130).

## Explicit non-scope

MBS-150 through MBS-154 remain open geometry-quality findings. No geometry formula or Blender render
contract is changed. MBS-136 remains open against a real provider, and paid Meshy work remains
prohibited.

## Normal launch

Double-click `Launch SkyForge Mesh Builder.command`.

## Exact release gates

Use `Run v0.7.1 Live Blender Preflight.command` and `Seal v0.7.1 for Claude.command` only in the
separately configured exact environments. The sealer fails closed and does not grant user-testing
clearance. Claude must review the exact checksum-bound candidate before any new user testing.
