# v0.8.1 Track S session-isolation status

This focused MBS-213 candidate separates the preserved historical multiview pilot workspace from new Track S experiments.

The original `mesh-builder/workspace/pilot_ui` workspace remains the read-only historical run. Starting Track S creates a new sibling workspace under `mesh-builder/workspace/pilot_ui_sessions/`, with a versioned run identity and active-session pointer. It does not copy or inherit the historical TaskLog, bundle approval, authorization preview, TOP/FRONT/RIGHT images, contact sheet, or provider state.

A new Track S run starts with no TaskLog state and no selected `inputKind`. Importing a valid three-quarter beauty reference constructs `SingleViewReconstructionInputV1`, resolves routing to `single_view_v1`, and appends the existing canonical `PREPARED` state. Human approval of that exact digest appends the existing canonical `BUNDLE_APPROVED` state. No lifecycle state or authorization/provider path was added.

Historical viewing is explicitly labeled and read-only. A historical `BUNDLE_APPROVED` state cannot define or authorize the active Track S run. Historical rejected MBS-195 authority files remain directly inspectable through the guarded read-only view route even when the historical bundle fails current validation.

The Track S run remains unsendable: the retained contract snapshot is stale, failed-task charging remains `UNRESOLVED`, and no run-specific preregistration exists. No API key, provider request, contract freshening, task, download, credit use, merge, tag, release, or resumed human testing is authorized by this record.
