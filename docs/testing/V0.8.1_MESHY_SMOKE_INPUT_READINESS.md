# v0.8.1 Meshy Smoke Input Readiness

Status: **BLOCKED ON HUMAN-APPROVED AUTHORITY BUNDLE**.

The repository was inspected for a genuine, human-approved, same-craft `top` / `front` / `right` `MultiviewAuthorityBundleV1`. None exists. Existing multiview bundles and images are generated test fixtures. Existing approved gunship evidence is single-view material and is not a substitute. No fixture was promoted, and no front or right view was fabricated.

The remaining prerequisite before any live authorization is a human-approved top/front/right authority bundle binding the exact asset and profile, ordered view roles, file SHA-256 values, dimensions and modes, bundle digest, human approval record, contact sheet, and source provenance.

The dry-run projection is recorded in `V0.8.1_MESHY_SMOKE_DRY_RUN_PREVIEW.json` with a null profile and digest and an explicit missing-bundle state. It is not sendable. No API key is required or read to create or inspect that projection.
