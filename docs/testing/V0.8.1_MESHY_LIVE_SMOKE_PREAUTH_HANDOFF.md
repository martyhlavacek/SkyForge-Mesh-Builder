# v0.8.1 Meshy Live-Smoke Preauthorization Handoff

No Meshy smoke call is authorized. Do not enter or use an API key, submit or poll a provider task, download provider artifacts, consume or purchase credits, or begin provider-result visual testing under this handoff.

The preauthorization candidate configures exactly `assets.meshy.ai` and the operating system DNS resolver under `skyforge.meshy-artifact-hosts.2026-08-04.v1`. Every other hostname and every unsafe or mixed DNS result fails closed. This prepares policy review; it does not authorize a provider operation.

1. Complete independent security review of the re-verified contract, exact host set, operating-system resolver, redirect rules, no-download preflight, and request projection.
2. Supply one genuine human-approved same-craft top/front/right authority bundle. None exists in the repository as of this candidate; fixtures and single-view evidence must not be substituted.
3. Bind the exact asset, supported profile, ordered roles, file hashes, dimensions and modes, bundle digest, approval record, contact sheet, and provenance.
4. Re-run every preauthorization gate against the exact bound bundle and candidate.
5. Obtain separate explicit Marty authorization for one geometry-only task with a maximum 20-credit cap.

Completing these prerequisites does not itself authorize the task. The authorization must bind the exact bundle digest, verified provider contract, exact approved-host configuration, resolver configuration, request preview, and credit cap. A second task requires separate authorization.

MBS-182 remains open as an Observation. MBS-183 remains open until security review and the missing authority bundle prerequisite are satisfied. MBS-136 remains open against a real provider. No tag, release, user testing, second provider, or Alpha 2 work is included.
