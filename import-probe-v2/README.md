# Independent VMP v2 Import Probe

This consumer-side probe validates external/manual VMP v2 archives without importing Mesh Builder code or schemas.
Its schema copies are pinned by `SCHEMA_SHA256SUMS.txt`. Historical VMP v1 remains in the separate, frozen
`import-probe/` tree.
